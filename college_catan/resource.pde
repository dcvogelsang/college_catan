class resource{
  //PImage card;
  String type; //if type null, refering to a place not on board and should do nothing
  
  resource(String t){
    type = t;
    String cardName = type + ".jpg";
    //card = loadImage(cardName);
    
  }
  
  void display(float x){
    //image(card, x, height - 150);
    
    
  }
  
}